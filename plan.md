# Plan

- [x] Inspect the existing delay_alloc clear_file theft test and the pbt-native CMake target wiring.
- [x] Implement or repair the delay_alloc clear_file property tests so they exercise the intended oracle on eval/delay_alloc/optimization/lowlevel_file.c.
- [x] Build and run the new `delay_alloc_clear_file_pbt_test` target to verify the property suite compiles and passes.
