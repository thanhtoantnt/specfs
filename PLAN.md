# Plan

- [ ] Inspect existing checksum-related PBT harnesses and confirm the correct target/source mapping for the requested checksum test.
- [ ] Create or adapt the theft-based property test harness for the checksum contract on the intended lowlevel_file implementation.
- [ ] Update `pbt-native/CMakeLists.txt` with the checksum target and verify the new/updated test compiles conceptually with the current harness.
